synthasize text to speech with a matching subtitle file using the edge_tts python package

to install: 
- clone the repo and cd into in
- make a virtual python environment 
- pip install -r requirements.txt
- run main.py